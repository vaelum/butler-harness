# butler configuration for %%NAME%%.
#
# Every section is optional; a component's commands appear only when it is
# declared. `python butler.py --help` shows what this file produced, and
# `python butler.py doctor` says whether the tooling for it is installed.

[project]
name = "%%NAME%%"
dist = "dist"
